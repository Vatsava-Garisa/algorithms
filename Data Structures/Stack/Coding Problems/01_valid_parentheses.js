
/**
 * Given a string containing only '(', ')', '{', '}', '[', and ']', determine if it’s valid.
 * The string is valid if brackets are closed in the correct order.
 * Input: "({[]})"
 * Output: true
 */

